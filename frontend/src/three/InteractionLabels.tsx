import { memo } from 'react'

/**
 * Placeholder for 3D or overlay labels on interactive objects. Extend when needed.
 */
function InteractionLabelsInner() {
  return null
}

export const InteractionLabels = memo(InteractionLabelsInner)
