import { memo } from 'react'

/**
 * Placeholder for volumetric light beams. Extend with god rays or light shafts when needed.
 */
function VolumetricLightsInner() {
  return null
}

export const VolumetricLights = memo(VolumetricLightsInner)
